/**
 * A type alias representing numeric values.
 * This alias allows for flexibility in handling different numeric types,
 * including standard JavaScript numbers, bigints for large integers, and strings
 * that represent numeric values.
 *
 * @typedef {number | bigint | string} Numeric
 */
export type Numeric = number | bigint | string;
